<!-- SPDX-License-Identifier: LicenseRef-EverseLife-Content
     Copyright (C) 2026 Nurlan Urazkulov -->

# План: карта как одно пространство — глобус, география и память

> **Статус:** реализовано · программа принята решением
> [D-319](02-decision-log.md) от 2026-09-05 и переиграна
> [D-321](02-decision-log.md) от 2026-09-06 в части раскладки: поверхность не
> закладывается, её находит разведка из скрытого поля. Волны 0 (вольт),
> 1 (сервер: география), 2 (память и публичная карта), 3 (клиент: проекция),
> 4 (клиент: полосы масштаба, земля, обрубки), 5 (подлёт), 6 (уборка) и
> 7 (карта-предмет) исполнены и влиты в `main` обеих копий (код —
> `fd24a2d`); ход волны — в §6. Масштаб неба закрыт D-320 (§2, п. 2).

Разговор владельца 2026-09-05. Три слоя карты — космос, планета, город —
сливаются в **одно непрерывное пространство**: планеты становятся шарами,
которые можно вращать, приближение показывает города и узлы, отдаление — систему.
Слой «локация» и борт корабля из пространства выведены в отдельное окно.

Документ описывает модель, волны и дыры. Решения о механике — в D-319; числа —
в реестре констант ([30-economy/07](../30-economy/07-constants.md)), здесь только
их имена и стартовые значения (D-065).

---

## 1. Что решено владельцем

| Вопрос | Ответ |
|---|---|
| Три слоя | Одно пространство; на поверхности один уровень графа, город — метка группы; субузлы — окном |
| Вращение | Только по широте и долготе, север всегда вверху — D-237 не нарушается |
| Время и расстояние | Связаны везде, и в городе тоже: время ребра выводится из расстояния |
| Единицы | Одна ось для поверхности; небо — честный масштаб, но **позже** (соседняя сессия) |
| Подлёт | Камера наклоняется по ходу приближения от эклиптики к поверхности |
| Видимость | **Радиус видения** от тела плюс **память**: последние X мест, где игрок был. У узла нет свойства «разведан» — ничто не прячется, видимость — свойство смотрящего |
| Память | Бессмертна, как знания персонажа (инвариант I8); старое постепенно забывается. Виденное сейчас — ярче, взятое из памяти — темнее |
| Разведка | **Пропадает как механика.** Граф планеты — узлы и рёбра — существует целиком с рождения мира и проходим весь; открывается ходьбой |
| Дороги | Бездорожье медленнее, чем сейчас; **тропинка** протаптывается частой ходьбой и зарастает без неё |
| Рельеф | Горы, реки, озёра, океаны — поле планеты; узлы берут из него свойства, глобус его рисует |
| Публичная карта | Снимок состояния мира с задержкой в несколько реальных суток: нечестно видеть, что происходит на другом конце мира |
| Климат | Поле по широте и долготе; планеты вращаются вокруг оси, местное время; тень на глобусе |
| Субузлы и борт | Отдельное окно с плоской раскладкой |
| Старый мир | Перезапуск с нуля, миграции мест нет |
| Производительность | Рассматривается позже |

---

## 2. Модель пространства

### Единицы и координаты

1. **Поверхность — метры.** У каждого узла слоёв «планета» и «город» — широта
   и долгота на своей планете (`node.properties["map"]` меняет форму:
   `lat`, `lon` вместо `x`, `y`). Радиус земли под ногами в километрах —
   корень из `planet.land_area_share` на `planet.earth_radius_km` (D-324).
   ~~То же число, что решает тяжесть и плотность, решает и поверхность~~ —
   с D-324 **не то же**: тело в небе (`planet.radius`, масса, плотность) и
   земля под ногами разведены, и радиус поверхности Терры 99,6 км против
   6 371 км её тела. Расстояние между узлами — дуга большого круга по земле.
2. **Небо — свои единицы, и это не временно (D-320).** Орбиты неба честны:
   Кеплер III сходится до 0,02%, и единица карты — это ≈ 1,1 млн км
   (`map.sky_unit_km`). А тела в небе нарисованы **в 86 раз крупнее натуры**
   (`orbit.body_radius` = 0,5 ед. на радиус Терры против настоящих 0,0058), и
   D-320 объясняет, почему это не поправить: тик симуляции шагает минуту, и в
   истинном масштабе корпус пролетал бы планету насквозь между двумя
   выборками. Значит, «честный масштаб с перенастройкой перелётов» из первого
   разговора невозможен без дробления шага, и стык устроен иначе: **полоса неба**
   рисует тело в 86 раз, **полоса поверхности** — настоящий шар
   `planet.land_area_share` на `planet.earth_radius_km` (D-324), а хореография подлёта (ниже) не
   переходит из одной системы координат в другую, а **сжимает тело** с маркера
   до натуры по ходу спуска. Между двумя полосами нет физической непрерывности,
   и это записано, а не спрятано.
3. **Город — кольца в метрах** вокруг центра (биопринтера): `MAP_STEP` и
   `MAP_MIN_GAP` из `runtime.py` становятся метрами и переезжают в вольт, потому
   что с географией они решают, сколько узлов уместится на планете, — это баланс
   (D-065), а не исполнение.
4. **Слой «локация» и борт — вне пространства.** Этажи вертикальны, отсеки лежат
   на своей сетке (`SHIP_GRID`), и ни у тех, ни у других нет севера. Они
   показываются **окном «Внутри»** — плоская раскладка тем же языком, что
   глобус, открывается с узла. Тело на летящем корабле стоит на глобусе на
   маркере корабля; комната видна в окне.

### Камера

- Состояние камеры — **широта, долгота, высота** над поверхностью и планета,
  к которой она привязана; в системном виде — точка эклиптики и высота.
- Рисование — ортографическая проекция, повёрнутая так, что центр кадра в
  нуле. Координаты у экрана всегда малы, каким бы ни был масштаб; это же
  снимает вопрос точности `viewBox` на тринадцати порядках.
- Север вверху всегда. Вращение рукой — только по двум углам, без инерции и
  автоповорота (D-318 разрешает непрерывное движение одной карте рубки).
- **Подлёт — хореография**: с высоты системы камера смотрит на эклиптику
  сверху; внутренняя граница полосы неба — парковочная окружность
  (`orbit.park_radii` = 3 радиуса нарисованного тела, D-324), внешняя граница
  полосы поверхности — `map.approach_km` над настоящей поверхностью; между ними
  камера за время приближения наклоняется к экваториальной плоскости планеты,
  встаёт севером вверх, а тело сжимается с маркера до натуры.
  *Волна 5 (2026-09-06):* внутренняя граница неба в коде — масштаб кадра
  (`SKY_BOUNDS.nearest`), не парковочная окружность: `orbit.park_radii`
  остаётся числом рубки и симуляции, а маркер планеты на карте — глиф
  D-238, не `orbit.body_radius`. Стык — два диска одного размера, дальше
  спуск по масштабу с наклоном к точке стояния; см. «Ход волны 5».
  Ось планеты считается перпендикулярной эклиптике (наклон оси — не в этой
  программе).
- Метки не растут с зумом (как на карте рубки).

### Один уровень графа на поверхности

Уточнение владельца 2026-09-05 (второй разговор): слоя «город» в графе больше
нет. **Все узлы поверхности планеты — один уровень** (`Layer.PLANET`): дом на
окраине, рынок, жила в тайге и лагерь разведчиков стоят в одном графе с одними
координатами и связаны напрямую. Город остаётся **меткой группы** — узлы, чьё
`parent` — город, — и метка нужна для владения, закона, налога и колец D-089,
а на карте она показывает, **где город заканчивается**: граница группы
обводится.

Уровней у графа три, и только один из них рисуется на глобусе:

| Уровень | Что в нём | Где показан |
|---|---|---|
| `SPACE` | планеты как тела, делегаты кораблей в рейсе, на стоянке и в хватке (D-316) | системный вид: от парковочной окружности и выше |
| `PLANET` | все узлы поверхности | глобус |
| `LOCATION` | субузлы: этажи (D-247), отсеки борта (D-201) | окно «Внутри», на глобусе никогда |

На отдалении город **схлопывается в точку биопринтера** с именем «Город
такой-то»; рёбра, выходящие из любого узла группы наружу, рисуются выходящими
из этой точки. Это то, что сегодня делает `delegate` в `map/model.ts`, только
как чистая проекция показа, а не уровень графа. Переключатель слоёв уходит;
кнопки лупы остаются (телефон, D-238). `sceneKey`, `Layer.CITY` и раздельные
соседства раскладчика (`places._neighbourhood`) исчезают.

**Ворот нет (OQ-143 закрыт D-319).** D-206 разрешало ребро между узлом города и
внешним только у ворот — оттого путь к жиле шёл через город к выходу, что и
было поводом разговора. Правило снято: ребро наружу законно из любого узла
города. Таможня D-123 живёт на переходе (смена города у ребра), осада —
удержание всех рёбер границы, находка вешается туда, откуда вышли. Космодром
остаётся станцией и единственной дверью для кораблей (D-176, D-201). В коде
уходят свойство `выход`, `_mark_gate` в `city/founding.py` и проверка двери в
`connect` из `engine/travel`; таможня уже считает по смене города и правок смысла не ждёт.

---

## 3. География

### Поле климата

Планета при рождении мира получает **детерминированное поле** по широте и
долготе: широтные пояса температуры плюс шум по координатам от зерна мира.
Узел при рождении **читает** своё значение из поля, а не бросает кубик: `mean_temperature`, осадки и всё, чем сегодня дрейфует D-262,
становятся выборкой поля в точке. D-261 и D-262 остаются в силе как чтение
климата земледелием; меняется источник числа.

Глобус рисует поле как **приглушённые пояса тона** — палитра D-238, без текстур.
Это же закрывает вопрос «гость видит точки на голом шаре».

### Вращение и местное время

`time.day_*` уже задаёт планетарные сутки (Терра — 38 ч). Добавляется
**долгота**: фаза суток в узле — общая фаза планеты плюс долгота, делённая на
полный оборот. `climate.day_phase` получает долготу узла; `is_day`, `light_now`,
`temperature_now` за ней. У двух городов на одной планете день наступает в
разное время, свет на грядках — тоже.

На глобусе — **терминатор**: тень ночной половины, вычисленная из часов сервера
на момент отрисовки. Это не таймер и не запрос (D-226): рисуется то, что уже
известно, и перерисовывается с любым кадром. Терра поворачивается на четверть
градуса в минуту — движения глаз не видит, и П3 D-238 не задет.

### Разведка — генератор графа (D-321)

Первый замысел D-319 — все узлы и рёбра строками при рождении мира — упёрся в
размер планеты честного радиуса: 181 млрд узлов по правилу «у реки шаг 10 м»,
или сотни километров между узлами при любом хранимом числе. Граф при этом не
меняется и вычислим из зерна, так что хранить его незачем. D-321: поверхность
**не закладывается**, её находит разведка — и в момент находки узел становится
строкой.

**Скрытая карта.** Планета целиком описана полем от `terrain.seed`: рельеф,
вода, горы, климат, осадки, **биом** точки (`biome.*`). Разведчик тычет в
точку на глобусе, игра прижимает её к решётке `map.lattice_m` и **читает**
поле там: у реки — река, в горах — жила и холод, на экваторе вдали от воды —
пустыня с суточным перепадом. Ключ узла — ячейка решётки: два города,
разведывая навстречу, находят один и тот же узел (D-237).

**Ландшафт диктует, куда и как далеко.** Дальность — `biome.reach_m` биома
исходного узла (равнина 5–20 м, горы 20–100 м); цель на суше, прямой путь не
через воду; новый узел не накладывается на найденные (радиусы по площади
`explore.node_area`), новое ребро не пересекает существующих. В обжитом центре
разведать нечего; в глуши — сколько угодно.

**Цена — расстояние.** Разведать точку в *d* метрах стоит ходьбы *d* метров по
бездорожью и столько же выносливости — **в одну сторону**: разведчик туда
доходит и там остаётся (D-327). Занятие тела D-211, задание журнала; с похода
можно повернуть назад, и тогда тело возвращается в исходный узел (D-194). По
сроку узел появляется с ребром к исходному — и только к нему: дорога есть
чей-то труд, и сама собой между узлами не берётся (D-326, отменяет сшивание с
соседями в `biome.reach_m.max`).

**Комплексы.** С `complex.chance` находка — схема из `complex.schemes`: на
Авроре шахта Предтеч со складами руды, застава, редкий мёртвый город D-232 (все
помещения открыты); на Терре излучина, перевал, оазис. Внутренности — окном
«Внутри».

**Видимость — свойство смотрящего, не узла.** У узла нет флага «разведан»:
найденный существует и никак не прячется. Что игрок видит, решают **радиус
видения** вокруг тела — узлы в нём ярче — и **память** (§4) — узлы из неё
темнее. Ребро из видимого узла к невидимому рисуется обрубком в туман; рельеф
виден всегда. *Предложение:* радиус — расстояние `map.sight_km`, а не шаги
графа.

Что остаётся снятым с D-319: истощение D-156 и теснота D-207 как счётчики
(теперь это геометрия правила места), дрейф D-262 (теперь чтение поля);
**делянка** не ищется — кольца города заложены (`city.ring_slots_base`);
**город** закладывается на любом найденном диком узле; **жилы** лежат там, где
положил рельеф. **Аврора — как любая планета**: её комплексы находит разведка,
а не сид, и бросок «найти комнату» D-232 не возвращается — помещения комплекса
открыты с рождения.

### Тропинки

Лестница покрытий D-107 получает ступень снизу:

| Покрытие | Код | Время | Как появляется | Как исчезает |
|---|---|---|---|---|
| **Бездорожье** | `Surface.WILD` (новое) | `road.wild_multiplier` | заложено при рождении | — |
| **Тропинка** | `Surface.TRAIL` (смысл меняется) | `road.trail_multiplier` | протоптана: износ ребра ≥ `path.wear_threshold` | заросла: износ < `path.fade_threshold` |
| **Дорога** | `Surface.ROAD` | `road.road_multiplier` | работа (D-107) | `road.decay_rate` |
| **Мощёный тракт** | `Surface.PAVED` | `road.paved_multiplier` | работа | `road.decay_rate` |

**Износ — счётчик на ребре.** Каждое прибытие по ребру прибавляет единицу:
пишет **задание прибытия воркера**, не `look`, SQL-выражением
`wear = wear + 1` без чтения (правило CLAUDE.md о счётчиках; тест-гонка на
две сессии обязателен). Суточный тик снимает `path.fade_per_day`, не ниже
нуля. Покрытие бездорожья и тропинки — **функция износа**: поднялось выше
порога — тропинка, опустилось ниже нижнего порога — бездорожье; два порога, а
не один, чтобы ребро на границе не мигало. Дорогу и тракт износ не трогает:
у них своё состояние и своя порча (D-107).

Транспорт по-прежнему не идёт по бездорожью (D-107); идёт ли он по тропинке —
нет: тропинка — пешая, иначе дорога как работа теряет смысл.

### Рельеф и вода

Поле планеты шире климата: **высота и вода**. При рождении мира от зерна
строятся континенты и океаны (порог высоты), горы (высота выше порога), реки
(линии стока от истоков по градиенту к воде) и озёра (замкнутые низины). Из
этого поля:

- **климат** читает высоту и близость воды, а не только широту;
- **узел** при раскладке берёт свои свойства из поля в точке: узел в
  `terrain.river_reach_km` от реки получает `water = river` (метка уже есть в
  `engine/world/land.py`) **всегда**, а не с вероятностью — иначе река на
  карте лгала бы; узел несёт жилу с долей `ground.vein_share ×
  biome.vein_k` своего биома (в горах чаще), на равнине — с обычной;
- **глобус рисует поле**: океаны и озёра — заливкой, реки — линиями, горы —
  тоном по высоте; всё палитрой D-238, без текстур, векторно (SVG-пути).
  Рельеф — данные каталога планеты, `/public/terrain/<planet>`, отдаётся раз и
  кэшируется (D-225: константа мира, не `look`).

Так у поиска появляется **стратегия до находки**: фермер идёт вдоль реки,
шахтёр — в горы, и оба знают почему. Публичная карта с задержкой (§5) этого не
портит — рельеф публичен с рождения мира, узлы приходят к гостю с задержкой.

**Цена.** Реки — самая дорогая часть генерации: честная гидрология с
заполнением низин — отдельная задача; стартовая версия — линии стока от
заданного числа истоков без заполнения, и для меток с показом её хватает.
Объём рельефа на телефоне — несколько сотен путей на планету; упрощение по
масштабу — вопрос производительности, отложенный владельцем (§1).

### Время и расстояние

Время ребра на слое планеты — `расстояние / travel.walk_speed_kmh ×
коэффициент покрытия` (D-107). `explore/run.py:350` перестаёт бросать
`dice.uniform(step.min, step.max)`.

**Связь действует везде, и в городе тоже (OQ-139 закрыт D-319).** Владелец:
все узлы на одном уровне, и правило одно. Прежние секунды шага и пути через
город перестают быть константами — время шага по городу выводится
из `map.city_step_m` и `travel.walk_speed_kmh`, и оба ключа уходят из реестра
волной 1 вместе с кодом. Что это значит в числах при 4 км/ч:

| Шаг кольца | Шаг по времени | Центр → окраина (5 шагов) | Сегодня |
|---|---|---|---|
| 150 м (как `MAP_STEP`) | 135 с | 11 мин | 4–10 с · 20–60 с |
| **30 м (предложение)** | **27 с** | **2,3 мин** | |
| 12 м | 11 с | 54 с | |

Меньше 30 м не выходит: площадь узла `explore.node_area` — 120–600 м², то есть
сторона участка 11–25 м, и при шаге 12 м участки лягут друг на друга.
Стартовое `map.city_step_m` = 30 — втрое медленнее нынешнего города, но всё
ещё «почти бесплатно» по D-089: центр и окраина в паре минут. Число правится по
D-065; D-089 в части «шаг — секунды» становится следствием геометрии, а не
правилом.

---

## 4. Память

Память места — **знание** (`knowledge`, `kind = place`): привязана к личности, не
к телу, и не отнимается ни смертью, ни судом, ни городом (I8). Записывается
заданием прибытия воркера — не в `look` (чтение не пишет). У записи — время
последнего визита; повторный визит обновляет его.

**Потолок X = `map.memory_places`, вытеснение по свежести.** Сверх X забывается
самое давнее. В показе два тона, не шкала: узел в радиусе видения — ярче,
узел из памяти — темнее; возраст памяти не показывается.

**Что видно.** Публичные города и магистрали планеты, на которой стоишь
(D-097), плюс память, плюс радиус видения вокруг тела (его же, придя, тело и
запомнит). Рёбра из видимого узла к невидимому — обрубки в туман. Запомненный узел показывается **таким, каким его знает сервер
сейчас** — имя, тип, рёбра. Снимок «как было» не хранится: это был бы второй
мир рядом с миром (аргумент D-240 остаётся верным для снимков). Тёмный тон памяти — то самое «данные устарели», что стояло в
[50-interface/05](../50-interface/05-map-and-movement.md) до D-240.

**Закреплённого в памяти нет (OQ-142 закрыт).** Дом, участок и город
гражданства забываются наравне со всем: всегда вытеснение по свежести за
потолком, одно правило без исключений.

**Карта как предмет остаётся (OQ-141 закрыт).** Память забывает, карта
запечатляет навсегда: вещь, в которую переносится часть памяти, и которая
показывает свои узлы, пока она в руках, — с пометкой «данные устарели», потому
что карта рисована в свой день. Так торговля картами D-097 получает вещь, а не
пересказ. Правило одно и решено владельцем: **память забывает, карта
запечатляет навсегда** — остальное исполнение, а не вопрос (OQ-145 закрыт):
карта рисуется из памяти рисовальщика на день рисования и держит свои узлы с
рёбрами, пока существует как вещь; лежит в кармане — её узлы видны на глобусе
тоном памяти с пометкой дня; читается — ничего в память не переносит, иначе
забылась бы снова. Подделка карт — тема Авроры (10-world/05), не этой волны.

### Расчёт X

Ограничивает не сервер, а телефон.

| Что | Оценка |
|---|---|
| Узел на проводе (`MapNode`) | ≈ 250 байт |
| Рёбер на узел | ≈ 1,5, по ≈ 80 байт |
| Элементов SVG на узел | 3 (метка, знак, имя) + 1,5 ребра |
| Бюджет ответа карты на мобильной сети | ≈ 150 КБ |
| Бюджет элементов SVG на телефоне при перепроекции рукой | ≈ 2 000 |

Оба бюджета дают **X ≈ 400**. Сервер при этом хранит 400 строк на личность
(10 000 личностей — 4 млн строк с индексом по личности), пишет одну строку и
удаляет одну на прибытие, отдаёт карту одним запросом с соединением
`knowledge → node → edge`. Нагрузки не видно; если она появится — сначала
дельта карты вместо целой, потом X. Стартовое значение 400, правится по D-065.

---

## 5. Публичная карта

`/public/map` без токена отдаёт небо (как D-240), рельеф и **снимок
поверхности, каким мир был `map.public_delay_days` суток назад**: все узлы —
они существуют с рождения и не прячутся, — но их состояние, города, дороги,
постройки и границы — с задержкой. Так гость и игрок с двумя вкладками не видят,
что происходит на другом конце мира сейчас: это было бы нечестно к тем, кто
там.

Механизм — **снимок, а не фильтр**: суточный тик воркера пишет снимок публичной
части графа каждой планеты (узлы, рёбра, покрытие, группы, границы), а маршрут
отдаёт самый свежий снимок, которому не меньше `map.public_delay_days`. Ни
одного нового поля на узле; снимки старше задержки удаляются тем же тиком.

---

## 6. Волны

Каждая — своя ветка кода и ветка вольта, пара «код + сборка» вливается одним
движением (CLAUDE.md). Волны 1 и 2 независимы от клиента и идут первыми.

| Волна | Что | Где в коде |
|---|---|---|
| **0. Вольт** | D-319, OQ-139…138, этот план, константы в реестре, правки 05-map, 07-topology, 06-space | вольт |
| **1. Сервер: география** | `places.py`: широта/долгота, посадка в касательной плоскости, зазор по дуге; `models/world.py`: `Layer.CITY` уходит, город — метка группы; ворота D-206 сняты; `climate.py`: поле и долгота в фазе суток, суточный перепад; `world.yaml`: координаты посевных городов; `seed.py`: рельеф и поле климата, **без раскладки поверхности** (D-321); `engine/biome.py`: класс точки поля; `engine/explore/` — новая разведка: цель на решётке, правила дальности, направления и места, срок из расстояния, материализация из поля, комплексы по схемам; `lot` — свободное место кольца, основание города — на любом найденном диком узле; `engine/road.py`: `Surface.WILD`, износ и два порога; `connect`: время из расстояния; `ship/flight.py`: посадка занимает площадь узла, отказ на выборе с орбиты | `engine/places.py`, `engine/climate.py`, `engine/terrain.py`, `engine/biome.py`, `engine/explore/*`, `engine/travel/*`, `seed*.py`, `models/world.py` (`Surface.WILD`, `edge.wear`), `engine/road.py`, `worker` |
| **2. Сервер: память и публичная карта** | `KnowledgeKind.PLACE`, запись в задании прибытия, вытеснение; `sight.py`: радиус видения `map.sight_km` + память, два тона на проводе; снимки публичной карты суточным тиком, `/public/map` отдаёт снимок старше задержки |
| **3. Клиент: проекция** | Модуль проекции рядом с `camera.ts`: широта/долгота → экран, скрытие обратной стороны, дуги рёбер и обрезка на горизонте; `hand.ts`: перетаскивание вращает; камера — широта/долгота/высота | `panels/map/camera.ts`, `hand.ts`, новый `globe.ts` |
| **4. Клиент: полосы масштаба и подлёт** | Порог высоты вместо `Switcher`; хореография наклона; поле климата и терминатор; два тона по `faded`; обрубки рёбер в туман (сервер отдаст ребро с одним видимым концом); окно «Внутри» для этажей и борта | `GraphMap.tsx`, `map/Nodes.tsx`, `map/Sky.tsx`, новый `Inside.tsx` |
| **5. Небо** | Стык полос: маркер в 86 раз в полосе неба, сжатие до натуры на подлёте, хореография наклона от эклиптики к экватору (из волны 4, где переход сделан срезом), корабли на стоянке и в хватке — только в полосе неба. Перенастройки перелётов **нет** — D-320 закрыл вопрос честного масштаба тел разрешением симуляции | `map/orbits.ts`, `globe.ts`, рубка не трогается |
| **7. Карта-предмет** | Вещь, рисуемая из памяти рисовальщика на день рисования (D-319 п. 6, OQ-145): держит узлы с рёбрами, пока существует, из кармана видна тоном памяти с пометкой дня, чтение в память не переносит | `engine/sheet.py`, `engine/mapshot.py`, `api/commands/map.py`, `market/counter.py`, новая вещь в вольте |
| **6. Уборка** | `Switcher`, `sceneKey`, `map/layout.ts`, `MAP_SIGHT*`, `dice.uniform` времени ребра; тесты `test_places`, `test_sight`, `test_reads` (`READS`); `test_explore*` уходят с пакетом | везде выше |

Перезапуск мира — между волнами 2 и 3: новая схема мест несовместима со
старой, а миграции нет по решению владельца.

### После волн: вход в мир на глобусе (2026-09-06)

По слову владельца экран входа и регистрации разделён надвое: одна
половина — **глобус планеты** из публичной карты (`/public/map` без жетона:
небо и снимок поверхности с задержкой, D-319 п. 7) с землёй, городами и
дорогами, медленно вращается сама, пока рука не возьмёт (на шаге двери
стоит), приближается колесом, палец скроллит страницу; суша доведена до
горизонта (дальние углы клетки выталкиваются на лимб), швы между клетками
закрыты обводкой в свой тон; вблизи земля
читается между клетками сетки (полклетки), чтобы берег гнулся, а не
ступенчатился; имена городов видны с масштаба, где читаются; другая
половина — форма входа или шаг регистрации. На четвёртом шаге (выбор принтера, D-013) двери
`/public/doors` ложатся на глобус точками — ряд двери несёт `planet` и
`place`, — щелчок по точке открывает карточку двери во второй половине,
как раньше открывались карточки списком. Почта проверяется на первом шаге
командой `join.check` до идентификации — тем же отказом `cmd-email-taken`,
что даёт `join`, а не на четвёртом. Публичная карта у анонима кэшируется
браузером на `PUBLIC_MAP_MAX_AGE_S`, и глобус входа может отставать на
эти минуты — как и сама публичная карта.

Проверка своими глазами (2026-09-06, Chrome при масштабе 150 %) нашла три
причины «плохого качества», которых из кода не видно. **Диск меньше шара**:
браузер читает `r`/`cx`/`cy` окружности как CSS-длину и насыщает её на
2²⁵ устройственных пикселей, а радиус планеты в единицах карты — 32 млн:
на любом экране с масштабом больше единицы море и обрезка земли
рисовались на ⅔ радиуса, а пути — целиком. Диск и обрезка стали путями
(`diskPath`), глобус входа рисуется в своих малых единицах (`DISK`), и
только рука переводится обратно в единицы карты. **Веер линий на полюсе**
— реки: истоки раздвигались по клеткам сетки, а полярный ряд — сто
восемьдесят клеток вокруг одной точки, и полярная шапка выпускала по реке
из каждой третьей; истоки раздвигаются по дуге (`SOURCE_SPACING_DEG`,
5°), и веер из двух десятков сжимается до трёх рек с шапки (на 85°
широты по дуге помещается до шести истоков). Реки того же зерна от этого
другие — мир между волнами всё равно перезапускается; что делать с
найденными узлами, когда алгоритм поля меняется у живого мира, — OQ-149.
Тем же днём по слову владельца реки с глобусов сняты вовсе: волосяная
линия поверх тона читается царапиной, а не рекой. Это только картинка —
поле рек живёт на сервере (метка `water = river` у узла, брод, «река не
пересекается напрямую» по D-321), и `/public/terrain` их по-прежнему
отдаёт; клиент их не рисует. Сглаживание берега — тоже только картинка:
сервер и до него читал высоту между центрами клеток (`Field.height`), так
что линия берега на глобусе теперь совпадает с тем, что разведка считает
сушей, а не огрубляет её до клеток.

Вечером того же дня (D-322, дополнение): планеты в двадцать раз меньше по
поверхности, на вкладке карты космоса нет — отдаление кончается диском,
небо только в рубке; камера в обоих режимах держит планету в центре кадра,
привязка обратно доворачивает шар к телу. Клетка земли считается от
радиуса (`bands.groundOf`), высота подлёта — восемь радиусов.

Тем же вечером — вход разделён на пять шагов (D-187, дополнение): «биопринтер»
на глобусе и «город» карточкой его чисел, список дверей по именам снят (кроме
того, чего глобус не рисует), карточка названа именем города, линии выбираются
вкладками, размер точки растёт по логарифму числа граждан, метка на глобусе
берёт фокус и отвечает Enter (D-077). На телефоне глобус — фон всего экрана:
шаги плавают над ним, шаг биопринтера отдаёт экран планете, карточка города
поднимается поверх неё, «назад» с города возвращает к выбору точки. Причины и
границы — в дополнении к D-187; здесь только то, что это уже сделано.

Ночью того же дня — D-323, рельеф в два уровня: сетка 2° решает сушу и
море, местное поле шума с деталями в 3 км даёт горы и озёра долями суши
(`terrain.peak_share`, `terrain.basin_share`) и читается плитками
10°×10° по 0,1° с `/public/terrain/{планета}/{ряд}/{столбец}` — те же
числа под ногами разведчика и на глобусе. Клиент просит плитки под кадром
от шестнадцатой клетки и режет землю по уровням шума. OQ-146 закрыт.
Той же ночью по слову владельца сцена карты: вблизи — узлы города без его
абстрактной точки, издали — только города кругами по числу узлов, дикая
земля между ними не рисуется (кроме узла под ногами); озёра своей водой,
горы светло-серым, земля режется в любом кадре, город открыт с 1,8 км.
Круги закрытых городов растут с отдалением (60 % за октаву, до 80 px),
мелкие пропадают первыми (узел на 200 км ширины кадра); свободное колесо
держит середину без доворота.
Кольцо города снято; граница города — контур земли его узлов (круги по
площади, сшитые по шагу города, сглаженные), `map/territory.ts`; площадь
узла на проводе карты. Рост города дорогами — OQ-151.
Граница серым пунктиром; ползунок зума у правого края вместо кнопок лупы;
полоска над картой без рамки и подсказки.
Издали граница сплошная; подписи рёбер сняты, время хода при наведении;
`map.sight_km` 0,1; шаг города 7 м, зазор 6 м; прицел разведки касанием
земли (`geoUnder`, панель `Survey`). **Берег лесенкой** — клетка
режется по уровню моря и гор по высотам в углах (`above`), сетка
читается тем мельче, чем ближе (до тридцать второй клетки), а вращение
перерисовывается пятнадцать раз в секунду, не каждый кадр.

Утром 2026-09-07 — сверка плана с кодом нашла один недоведённый пункт:
§7 п. 12, **посадка выбирается на глобусе**, а консоль всё ещё выбирала
площадку списком. Сделано: в консоли корабля над планетой стоит глобус
(`panels/ship/Landing.tsx`, чистая часть — `ship/pads.ts`) — та же сфера,
что у карты и у входа, с землёй рельефа, ночью планеты, узлами и путями
публичной карты; на нём метками стоят горящие космодромы, которые отдал
сервер, а на планете, где садятся куда угодно (D-233, Пироксис), — каждый
узел поверхности, теперь своей строкой. Строка площадки несёт **свободную
землю** (`room`, `estate.free_ground` — то же число, против которого
меряется приказ), карточка — след корпуса (`footprint`); клиент вывести это
не может, карта знает только всю землю узла (D-225). Размер метки — корень
из свободной земли относительно наименьшей, метка, куда корпус не влезает,
рисуется пустой, в подписи — имя и свободные метры; так экипаж выбирает,
видя место, а отказ «нет места» остаётся на приказе (D-319). Первый кадр
открывается над выбранной меткой шириной в разлёт меток с запасом, колесо
приближает, рука вращает. Список рядом называет те же площадки для
клавиатуры (D-077: клавиатура и видимый фокус, о читалках решение молчит);
оба читают один выбор. Поверхность берётся из **публичной** карты без жетона: с орбиты глаз
не видит ни одного узла земли (`map.sight_km`), а что мир знает о
поверхности планеты — то, по чему и выбирают; площадка не двигается, и
задержка снимка ей не мешает; узел моложе снимка — найденный на Пироксисе
на этой неделе, верфь, зажжённая вчера, — в списке есть, на глобусе не
стоит, пока снимок его не догонит. (2026-09-08: половина этого отменена
дополнением к D-319 — консоль спрашивает обе карты и кладёт личную поверх
снимка, так что своя площадка, виденная или запомненная, стоит на глобусе
сразу; без жетона остаётся только чужое.) Подстановки сервера («один узел планеты»
из хода волны 1) больше нет: строки — сами узлы; пока карта не пришла или
ничего не разместила, выбирают из списка. Строка «посадка вслепую» снята из
локалей. Попутно снят дефект,
который проверка вскрыла первым же приказом: консоль корабля падала на
спуске и на подъёме — `course` в карточке такого корабля пуст, провод
пустые поля не отдаёт (`session._without_nulls`), а окно проверяло
`!== null` и читало поле у `undefined`.

### Ход волны 7 (2026-09-06)

Карта-предмет по §4 и D-319 п. 6 (OQ-145): **память забывает, карта
запечатляет**. Вещь — `map_sheet` («Лист карты»), чистый лист из ткани руками
(`gear`, 0,1 кг): сделан как любое снаряжение, носится, продаётся, теряется.
**Рисование** — команда `map.draw` на лист в руках: на него переносятся все
места из памяти рисовальщика (`engine/memory.known`) на момент рисования, за
`map.draw_stamina` — свою цену, а не ключ переписки D-148: один рычаг не
должен крутить два действия. Строка тела берётся и перечитывается до
проверок (`refresh ... with_for_update`), тест-гонка на две руки — в
`test_sheet`.
Рисованный лист не перерисовывают: карта держит свои места, пока цела;
пустая память — отказ, рисовать нечего. Запись — таблица `map_sheet`
(`item_id` → момент рисования, ключи узлов), живёт и гибнет с вещью
(миграция `d5f1a8c2e937`); журнал получает `map.drawn`, и по нему клиент
перечитывает карман и карту. **Показ**: личная карта берёт места с листов в кармане
тела как память — тёмным тоном, — и на строке узла, известного только по
карте — не в радиусе, не в памяти, не публичного, — отдаёт `drawn` — день
рисования по календарю планеты **этого узла** (D-225: с провода этого не
вывести); инспектор пишет «по карте, рисованной в сутки N: что здесь теперь
— карта не знает» — сутки считаются как на часах, с единицы. Место, которое и в памяти, и на карте, помечается памятью: карта
не старит того, что помнишь сам. **Чтение** ничего в память не переносит —
особой команды чтения нет вовсе: карта показывает с кармана; пришёл ногами
— запомнил, как всегда (D-319 п. 6). Лист на земле или в чужих руках
показывает свои места тому, у кого он в кармане, а не рисовальщику.
Инвентарь различает чистый лист и карту флагом `drawn` на строке листа и
даёт кнопку «Нарисовать карту». Подделка карт — тема Авроры, не этой
волны. **Стакан продаёт только чистые листы**: рисованную карту прилавок
не кладёт (`market/counter._stacks`), она передаётся из рук в руки — по
D-132 координаты идут на доверии, а D-047 о стакане для физического товара
к вещи, чья цена — знание, не применяется; записано дополнением к D-319. Не сделано: снимок «как было» — нарочно (§4: второй мир рядом
с миром), карта держит только ключи и показывает узел таким, каким его
знает сервер сейчас.

### Ход волны 6 (2026-09-06)

Уборка по строке таблицы и по долгам, записанным ходами волн 1, 4 и 5.
Из строки: `MAP_SIGHT*` и `dice.uniform` времени ребра ушли ещё в волнах
1–2, старые `test_explore*` — с пакетом; `Switcher` остался дверью «Внутрь»
и камерой (волна 4), `sceneKey` ключом сцены по полосе; `map/layout.ts` —
пружины, сажавшие узлы без места, — **снят**: место у каждого узла
серверное (D-237), узел без места не рисуется, корпус в небе кладут часы.
Из долгов: **прицел читает окно, а не планету** — `explore.window_km`
(2 км, много шире любой дали) вокруг точки прицела, выборка в SQL по
градусам через место узла с индексами по широте и долготе (миграция
`c3e9b2a7d415`; выражение индекса и запроса — операторами `->`/`->>`, не
подскриптом JSONB, иначе Postgres индекс не берёт, и это пришпилено тестом
через `EXPLAIN`; пути с хотя бы одним концом в окне проверяются на
пересечение с дочитыванием второго конца, путь с обоими концами снаружи
прицел не видит — записано в ключе, окно много шире любой дали и шага
кольца); **разведчик с
живым походом в путь не уходит** — уход есть второе дело (D-211), и
`depart` из `engine/travel` отказывает `travel-scouting` у двери, а не по возвращении
пустым походом; тело при этом стоит в исходном узле до самого срока — карта рисует его в пути, движок считает его дома (D-327, «что осталось нечестным»), и ничего другого
в узле ему не отказывают (`explore-scout-gone` остаётся на случай, когда
тело увели иначе — кораблём); **кости земли переименованы**
«explore.vein_share», «forest_share», «stones_share», «meadow_share»,
«vein_richness», «vein_stock» → `ground.*` (реестр и вольт одним движением,
сборка пересобрана); **схемы комплексов проверяются тестом** — планета и
биом каждой схемы существуют, иначе схема молча не выпадала бы никогда;
**пришвартованный корабль — метка на узле порта** (§7, п. 12): сервер
отдаёт `moored` на строке порта, у которого стоит корпус (`Ship.docked_node_id`
на узел поверхности), в личной карте и в снимке — клиент вывести этого не
может, у корпуса на проводе родитель — сфера планеты, и причал от стоянки
по нему не отличить (D-225); знак `moored` на карте;
*(2026-09-08: вторая половина этой причины верна теперь не для всех. Метка
`moored` осталась общей карте, куда `in_sight` не отдаёт ничего, — но тому,
кто стоит на причале или на борту, корпус приходит своей точкой: место
в зазор от причала, город причала родителем. Без этого корпус был на проводе
и не был на карте — трап вёл в узел, которого нечем нарисовать, и на борт
нельзя было ни зайти, ни сойти с него. §7 п. 12 это не меняет: на стоянке
и на орбите корабль по-прежнему не точка карты.)* `GraphMap` разрезан —
ходок ушёл в `map/useWalker.ts` (679 строк, планка 800). Тесты строки —
`test_places`, `test_sight`, `test_reads` (`READS`) — на месте с волн 1–2, в
`READS` разведка не входит: `explore.survey` пишет. Не сделано:
`explore.node_area` и `explore.fill_share` остаются разведке по смыслу;
имена узлов сида по-русски — общий долг волны IV D-251.

### Ход волны 5 (2026-09-06)

Стык полос сделан **спуском по высоте**, а не срезом двух непохожих
картинок. Небо открывает поверхность на том масштабе, где настоящий диск
планеты ровно с маркер у потолка неба (`openScale`: радиус короны маркера
на масштаб потолка, но не ниже пола с запасом `ABOVE_FLOOR`), и маркер
становится диском того же размера. Дальше — **спуск** (`descentOf`): между
полом полосы поверхности (`map.approach_km`) и масштабом, где глобус
заполняет кадр, доля спуска считается по логарифму масштаба, чтобы каждая
октава наклоняла одинаково, и режется на `DESCENT_STEPS` = 24 шага — это
факт кадра, а карта перерисовывается только по смене факта. По доле спуска
глаз наклоняется (`tilted`): у пола он стоит над полюсом — небо смотрит на
эклиптику с её севера, ось планеты перпендикулярна эклиптике, — и, спускаясь,
наклоняется к точке, где стоит тело, север всегда вверху, со сглаживанием на
обоих концах. Колесо ведёт спуск в обе стороны; щелчок по маркеру в небе
(«Раскрыть») открывает диск и **летит** к улицам: у камеры появился
`zoomToward` — погоня по масштабу в логарифме с той же постоянной, что у
погони по точке с постоянной скоростью в октавах (`ZOOM_PACE`), чтобы
наклон был виден, а не проскочен; погоня переживает срез сцены, но не руку —
и колесо при привязанной камере тоже её обрывает. Два тела сцены при этом
остаются двумя: в небе — **глиф** маркера (`SPHERE_R`, D-238), карта не
читает ни `orbit.body_radius`, ни `orbit.park_radii` (они — числа рубки и
симуляции), и потолок неба — масштаб кадра, а не парковочная окружность;
на поверхности — натура. Стык не физика, а два диска одного размера в один
момент (§2, п. 2).

**Отступления.** Высоты у камеры по-прежнему нет: состояние — масштаб, а
высота вольта переводится в масштаб одной функцией (`surfaceFloor`,
прямоугольный обзор), и это принято за исполнение §2 «Камера» без
перестройки `camera.ts`. Наклон идёт не «к экватору» (D-319 п. 8), а к
широте, где стоит тело: глаз полосы поверхности — точка стояния, и конец на
экваторе увёл бы тело из кадра. Во время наклона рука вращает шар только по
долготе, читая сдвиг на показанной широте; вертикальный сдвиг — дело спуска.
Щипок на глобусе вращает шар, как один палец, и приближает к середине.
Корабли: на поверхности их нет по устройству сцены (`visibleOf` берёт
только уровни поверхности); в небе до этой волны корпус на стоянке или у
причала **рисовался** рядом с маркером планеты (`useSky`, `BERTH`) вопреки
D-319 п. 10 — теперь сцена неба прячет корпус без рейса, остаются делегаты в
пути. Метка пришвартованного корабля на узле порта глобуса (§7, п. 12)
не сделана — долг волны 6. Земля на спуске читается через клетку
(`COARSE_STRIDE` = 3), реки не рисуются: пока диск меньше кадра, клетка —
пиксель, а каждый шаг спуска перерисовывает землю. Диск при открытии равен
маркеру только у Терры: у планет, чей диск был бы с маркер лишь ниже пола,
он открывается на зазор выше пола и чуть крупнее маркера.

### Ход волны 4 (2026-09-06)

По таблице, с двумя отступлениями. **Полосы** — `panels/map/bands.ts`:
три полосы вместо переключателя слоёв — небо, поверхность, нутро; кнопки
слоёв сняты, у переключателя осталась одна дверь «Внутри / Наружу» для
этажей и борта (отдельного `Inside.tsx` не понадобилось: нутро — та же сцена
без севера, `useScene` выбирает ей этажи). Город на поверхности — точка с
именем принтера, пока масштаб меньше порога `CITY_SCALE`; ближе он
раскрывается в узлы, и вокруг них пунктиром рисуется граница (`ring`).
Переход между полосами — **срез**, как и было решено в §2, п. 2: отдаление
до пола поверхности отдаёт кадр небу, приближение в небе к маркеру планеты
(`OPEN_REACH` от середины кадра) открывает её поверхность. Первое
отступление: **пол поверхности — целый диск планеты** (`SURFACE_BOUNDS`,
6·10⁻⁶), а не область в десятки километров, как стояло в первом заходе
волны: ортографическая проекция с отрезанным горизонтом честно показывает
шар, и небо открывает планету на масштабе глобуса (`GLOBE_SCALE`), так что
маркер сменяется диском — это и есть срез. Хореография наклона и сжатия
маркера в шар остаётся волне 5 (в строку 5 таблицы дописано). Пол полосы
поверхности — из `map.approach_km` вольта: у камеры пока нет высоты, только
масштаб, и высота читается как прямоугольный обзор с неё — кадр вмещает две
высоты (`surfaceFloor`); с 50 000 км это ≈ 1,1·10⁻⁶, диск Терры при этом —
восьмая часть кадра, дальше небо. Масштаб открытия глобуса из неба считается
от радиуса планеты (`globeScale`: диск в 1,2 высоты кадра), а не литералом
под Терру. Ключ `map.approach_km` объявлен в реестре кода, хотя сервер его
не читает — чтобы вольт сверял его при загрузке, как остальные. Небо
открывает планету и по колесу у потолка, и по панораме, поставившей маркер
под середину кадра; **чужую планету** ни колесо, ни щелчок не открывают —
её поверхности нет в ответе `/public/map` (D-240), и пустая сцена читалась
бы как сломанная карта, а не как место, куда ещё лететь.

**Земля** — `/public/terrain/{planet}` рисуется под узлами
(`panels/map/Ground.tsx`, арифметика в `map/relief.ts`): море — диск шара в
оттенке планеты, суша — клетки сетки высот плоским цветом, горы светлее,
озёра водой, реки линиями. Тон суши — по **полю климата**: эскиз рельефа
теперь несёт `warmth`, среднюю температуру каждого ряда сетки на уровне
моря (та же широтная кривая, что читает `climate_at`, без шума и без
высоты), а клиент делит ряды по тёплым краям строк тундры и тайги из `biome.zonal` (волна 4 плана 12; до неё — `biome.bounds.cold_c` / `cool_c`)
на три тона. Второе отступление — **числа показа**: пороги полос, длина
обрубка (25 м), шаги дуги — константы клиента, а не вольта: они решают,
как рисовать, а не как играть (D-065 — о балансе). **Терминатор** —
тень ночной половины из часов при отрисовке (`subsolar`: полдень в фазе
0,5 от эпохи мира, восток раньше, как в `climate.day_phase`); солнце над
экватором, наклона оси нет, так что край тени идёт через полюса. Не таймер
(D-226): пересчитывается с любой перерисовкой карты (поворот, push,
щелчок масштаба), а между ними тень стоит — четверть градуса в минуту глаз
не видит; момент берётся с часов клиента при серверной эпохе, как и небо
(`useSky`), а не «из часов сервера» буквально — сдвинутые часы игрока
сдвинут тень, и это принято как у неба. Реки и берега на сетке 2° — грубые у города (OQ-146): на масштабе
улицы вся сцена лежит в одной клетке одного тона; это и есть «гость видит
точки на голом шаре» закрытым, но не рельеф под ногами.

**Обрубки в туман** — сервер отдаёт в личной карте `stubs`: для каждого
ребра с одним видимым концом и другим на поверхности за взглядом — узел,
**азимут** и покрытие, и ничего больше: ни точки, ни расстояния (§4:
туман — свойство смотрящего, и обрубок не выдаёт того, что скрыл). Клиент
рисует от видимого конца 25 м дуги по азимуту тем же штрихом, что ребро,
вполсилы. Азимут округлён до градуса — выбор кода. У обрубка по природе
есть направление (D-319 п. 4), и два обрубка к одному скрытому узлу с двух
видимых концов пересекаются в его точке; туман хранит расстояние, и его в
строке нет. Два тона по `faded` — из волны 2, без изменений.

Проверено в браузере на стеке worktree: город раскрывается и схлопывается
по порогу, отдаление уходит в небо и обратно открывает Терру диском с
материками, реками и тенью ночи; колесо через переход не прыгает
(границы масштаба рука читает через ссылку на полосу, обновляемую
синхронно с переходом, — иначе один щелчок колеса между переходом и
перерисовкой зажимал глобус в границы неба). Что оставлено волне 5:
хореография, высота камеры вместо `scale`, щипок как вращение, и
стоимость земли — 16 тысяч клеток проецируются на каждый поворот глаза,
когда кадр шире клетки (`GROUND_SCALE`); ближе, у города, под кадром одна
клетка, и рисуется одна заливка её тона без сетки; если на слабом
устройстве вращение глобуса станет вязким — контуры береговой линии вместо
клеток. Ревью волны сняло дефект: полоса «нутро» переживала переход на
узел без этажей и оставляла пустую карту без двери — теперь полоса
выводится из того, есть ли нутро, а не хранится (`map/useBands.ts`, куда
ушли из `GraphMap` состояние полос, факты кадра и переход).

### Ход волны 3 (2026-09-06)

Проекция — `panels/map/globe.ts`: ортографическая, север вверху, точка
взгляда (`Eye`: широта и долгота) в начале координат кадра, так что числа у
экрана малы при любом радиусе (§2 «Камера»); обратная сторона не рисуется
(`project(...).front`), ребро — дуга большого круга, разрезанная на видимые
куски у горизонта (`arc`), рука вращает по двум углам и не выше 85° (`turn`).
Радиус — корень из `planet.land_area_share` на `planet.earth_radius_km` из `/public/constants` (D-324); пока
книга не пришла, сцена плоская, как была. Что отложено в волну 4 вместе с
полосами масштаба: диск горизонта (при радиусе в 3·10¹⁰ единиц он рисуется
только с высоты), высота камеры вместо `scale`, щипок двумя пальцами (пока
двигает кадр, а не вращает), переключатель слоёв. Проверено в браузере на
стеке worktree: сцены города и планеты проецируются, перетаскивание при
свободной камере вращает поверхность.

### Ход волны 2 (2026-09-06)

По таблице, без отступлений: память мест — `knowledge` вида `place`
(`engine/memory.py`), пишут задание прибытия и возврат разведчика, потолок
`map.memory_places` с вытеснением самого давнего одним `DELETE`; видимость —
радиус `map.sight_km` по дуге (с волны 9 плана ландшафта земля между может его укоротить) плюс один шаг графа (трап, коридор, дверь)
плюс память плюс публичные города и уложенные дороги планеты (`engine/sight.py`), на проводе
один флаг `faded` для тёмного тона (D-225: клиент не отличит память от
отсутствия); снимок публичной карты — суточный шаг `snapshot`
(`engine/mapshot.py`, таблица `map_snapshot`), `/public/map` без токена отдаёт
небо и снимок не моложе `map.public_delay_days`, старее отданного тик
удаляет. Карта-предмет (OQ-145) — волна 7 таблицы; решения хода волны записаны дополнением к D-319 от 2026-09-06.

### Ход волны 1 (2026-09-06)

Первый заход сделал раскладку всей поверхности и упёрся в размер планеты;
записи ниже о краях вокруг городов («map.region_km») и о зерне — история этого
захода, и D-321 их снял: края ушли вместе с раскладкой, зерно 20260906
осталось (столица на суше нужна и разведке). Дальше волна идёт по D-321:
разведка из поля, биомы, комплексы.

Сделано по таблице, с отступлениями, которые стоит знать следующим волнам:

- **Узлы кладутся краями вокруг посевных городов, а не по всему шару**
  (`seed_planets.sites`, «map.region_km» = 20 км, «map.spacing_share»).
  Ревью волны нашло, что 120 узлов, разложенных по всей Терре в натуральную
  величину, стоят в 780–3000 км друг от друга — месяц ходьбы на ребро и
  пятнадцать запасов выносливости; заложенная поверхность была бы
  непроходима. Круг радиусом 20 км вокруг каждого посевного города даёт
  соседей в часе-двух (50-interface/05), между краями пути пешком нет, а
  остальной шар — земля без узлов. Записано как **предложение исполнения**:
  радиус, доля шага и само правило «край вокруг города» ждали подтверждения
  владельца или своего D-XXX. **Снято D-321** (той же ночью): дикие узлы
  рождаются разведкой, сид кладёт только посевные города и узлы веером
  вокруг якоря на дальность биома (`seed_planets.sites`); «map.region_km» и
  «map.spacing_share» из реестра ушли, подтверждать нечего.
  Тест-пин: каждое дикое ребро по силам телу (`body.stamina_max` против
  `travel.stamina_per_hour`). Замер на сборке 2026-09-06: Терра — 120 узлов,
  ребро по бездорожью 1,9–2,9 ч (по тропинке ~1,3 ч); Аврора — 28 узлов на
  три края, 7–10 ч; Пироксис — плато и три поля, 11–12 ч. Аврора и Пироксис
  выше «20 мин — 2 ч» интерфейса — это счёт узлов и радиус края, оба числа
  вольта.
- **Найденный узел безымянен, площадь — по выпаду** (владелец 2026-09-06,
  дополнение D-321): имя пустое, тип показывает знак карты по признакам
  (`vein`, `water`, `mountain`, биом), слово для отказов — `biome.names`;
  площадь — `explore.fill_share` свободного радиуса в пределах
  `explore.node_area`, зона запрета — круг узла. Долг имён D-251 этим снят с
  находок; у узлов `world.yaml` он остаётся. Прицел читает всю поверхность
  планеты (`explore.aim._surface`); обход соседей ради сшивания снят вместе с
  самим сшиванием (D-326), так что находка больше не стоит прохода по всем
  узлам планеты. Уход разведчика с исходного узла во время
  похода не запрещён (`depart` из `engine/travel`), поход при этом пропадает
  (`explore-scout-gone`). Схема с опечаткой в `planet`/`biome` молча не выпадает
  никогда (`spec.Shape` формы записи не проверяет).
- **Зерно рельефа сменено на 20260906**: при 20260905 столица (41°, 24°)
  стояла в море, и её край не имел суши ни под один узел. Координаты городов —
  владельца, зерно — нет; перебор дал первое зерно с сушей на весь край.
- **Ручьи бросаются по `site.river_share`** и при рождении мира: сетка
  рельефа — два градуса, край — двадцать километров, и река карты попадает в
  край одним краем из тридцати; без броска ни у одного узла не было воды.
  Река карты по-прежнему даёт воду всегда (`terrain.river_reach_km`).
- **Помещения посевных городов Предтеч** (Мерид, Кальдар, Вейр) открываются
  при рождении так же, как у заложенных сидом: команды «найти помещение» нет,
  и `ruins.city_rooms` иначе не исполнялся бы никогда.
- **Дикие рёбра пришиваются к узлам города, не к его делегату**: делегат —
  метка группы (§1), стоять на нём нельзя.
- **Космодром сида кроется крышей только над станками** (`seed_parts.buildings`).
  Раз посадка занимает свободную землю узла, а сид внутри города крыл узел
  целиком (двор D-246 без пустого; откат D-254 снял это для открытой земли),
  столичный порт не принял бы ни одного корпуса. Правило исполнения, не
  баланс: остальная площадь узла — перрон, который делит двор с домом и
  грядками по D-246. Порт, который строит игрок, регулирует площадь сам —
  домом, в котором стоит верфь.
- **Жеребьёвки посадки на Пироксисе больше нет** (D-235 в этой части снята
  D-319, дополнение в журнале от 2026-09-06): корпус садится в узел, названный приказом. Пока у клиента не было глобуса
  с выбором места, консоль показывала планету одной строкой, и её узел был
  подстановкой; с 2026-09-07 узел выбирают на глобусе консоли (см. «После
  волн»), каждый узел поверхности — своя строка со свободной землёй;
  отказ «нет места» приходит на выбранный узел.
- **`ship.node_area` — единственное число посадки:** след корпуса — сумма
  следов его узлов-построек («estate.hulls_footprint», «ship.hull_footprint»),
  новых констант нет. Земля занята с приказа: спускающийся корпус считается
  наравне с севшим, как заложенная стройка (D-218).
- **Узлы извержения Пироксиса** кладутся на шаг кольца (`map.city_step_m`) от
  плато, чёрные поля сида — через тот же выбор мест, что и дикие узлы
  (`seed_planets.sites`).
- **`MAP_SIGHT*` в реестре остаются** до волны 2: видимость по двум пройденным
  шагам сейчас, радиус `map.sight_km` и память — с `KnowledgeKind.PLACE`.
- **Долг имён:** доля жил переехала в `ground.vein_share` (см. выше), так что долг остался один — `explore.node_area` живёт под старым
  ключами (переименование — уборка волны 6); имена диких узлов сид пишет
  по-русски по биому (`biome.names` в вольте) — тот же долг волны IV
  D-251, что у всех имён сида, зачтён в `tools/check_copy.py`.

---

## 7. Дыры и допущения, оставшиеся после разговора

1. **Город и метры** — закрыт: связь везде, шаг кольца 30 м, см. §3.
2. **Полюса** — закрыт: полюса нужны, они видны при вращении и по ходу
   терминатора. Узлы не закладываются выше `map.city_lat_max`; камера не
   поднимается выше 85° широты, чтобы «север вверху» не вырождался (правило
   исполнения, не баланс).
3. **Небо** — закрыт D-320: честного масштаба тел не будет, единицы неба
   свои (§2, п. 2); стык полос сделан волной 5 как спуск между двумя дисками
   одного размера.
4. **Карты как товар** — закрыт: предмет нужен, память забывает, карта
   запечатляет; механика вещи — исполнение, см. §4 (OQ-145 закрыт).
5. **Закреплённая память** — закрыт: не закреплять, всегда вытеснение по
   свежести.
6. **Снимок против состояния.** Решено: состояние (§4). Записано, чтобы не
   переспрашивать.
7. **Публичная задержка и город** — закрыт волной 2: анониму всё с задержкой,
   города тоже; «сразу» город видит тело на планете (тёмным тоном, D-097).
8. **Скорость и покрытие.** `road.trail_multiplier` и `road.paved_multiplier`
   умножают время; при времени из расстояния они остаются множителями скорости
   и ничего не меняют.
9. **`explore.node_area`** — площадь найденного узла (60–240 м² с D-321);
   задаёт правило места: радиусы по площади не пересекаются.
10. **Разведка Авроры (D-232)** — закрыт D-321: комплексы Предтеч находит
    разведка по схемам `complex.schemes`, помещения открыты с рождения.
12. **Корабли на стоянке не рисуются вовсе** (уточнение владельца). Ни на
    глобусе, ни в полосе неба стоящий на орбите корабль не точка карты: в него
    попадают через окно рабочей станции «Космическая верфь» порта. На глобусе
    корабль стоит только пришвартованным к порту (узел поверхности, D-201).
    **Глобус с орбиты — это выбор места посадки.** Космодром и раньше выбирали
    уже с орбиты (10-world/06, D-245); теперь экипаж вращает планету под собой и
    выбирает: на Терре и Авроре — работающий космодром, на Пироксисе — любой
    узел поверхности (D-233), и раз узлы заложены заранее, видно их площадь —
    экипаж выбирает узел, где корпусу есть куда сесть. **Посадка занимает
    площадь узла** (подтверждено владельцем): корпус садится в узел, как
    постройка встаёт на участок, — `ship.node_area` на каждый узел борта из
    свободной площади узла, и узел без места отказывает **на выборе**, пока
    корабль ещё на орбите, а не после спуска (то же правило, что у погасшего
    маяка). Так у верфи появляется пропускная способность: космодром вмещает
    столько корпусов, сколько влезает в его площадь, и город растит порт
    площадью, а не флагом. Улетевший корабль площадь освобождает. В коде —
    `_will_take` / `land` в `engine/ship/flight.py` и `estate.built_area`;
    тест-гонка на две посадки в последнее место.
11. **Тропинки и обозы.** Тропинка пешая; если обоз по ней тоже не идёт, единственный
    способ возить — построить дорогу, как и сейчас. Записано как правило, не как
    вопрос.

---

## 8. Константы (предложение, D-065)

Имена — в реестр [30-economy/07](../30-economy/07-constants.md) волной 0;
стартовые значения правятся на живой игре.

| Ключ | Стартовое | Единица | Что решает |
|---|---|---|---|
| `planet.earth_radius_km` | 6371 | км | Мерка обоих размеров планеты (D-324): тело в небе — `planet.radius` от неё, земля под ногами — она же на корень из `planet.land_area_share`. Была planet.terra_radius_km = 319 (снята), и через неё поверхность считалась от небесного тела |
| `map.sky_unit_km` | 1 100 000 | км | Единица неба в километрах: **тело** Терры в небе — 6 371 км, то есть 0,0058 ед. (D-324), орбиты честны, тела нарисованы радиусом `orbit.body_radius` = 0,08 — в 14 раз крупнее натуры. Земля под ногами — другое число и другой ключ |
| `map.approach_km` | 800 | км | Высота, ниже которой камера в полосе поверхности; восемь радиусов **земли** Терры (D-324, было 2 500 при радиусе 319 км) |
| `terrain.detail_km` | 3 | км | Длина волны первой октавы местного шума (D-323): от гребня до гребня |
| `terrain.detail_seed` | 94 | смещение | Смещение зерна местного шума, подобранное под столицу (D-323) |
| `terrain.detail_amplitude` | 0,4 | доля подъёма суши | Размах местного рельефа поверх высоты сетки (D-323) |
| `terrain.peak_share` | 0,08 | доля суши | Какая доля суши по местному шуму — гора, где бы ни стояла (D-323) |
| `terrain.basin_share` | 0,03 | доля суши | Какая доля суши по местному шуму — озеро на планете с морем (D-323) |
| `map.city_step_m` | 7 | м | Шаг кольца города (был `runtime.MAP_STEP` = 150 условных); было 30 м, с D-323 (дополнение) узлы города в секундах ходьбы друг от друга |
| `map.min_gap_m` | 6 | м | Зазор между узлами — по площади участка, а не по ширине подписи; было 20 м (D-323, дополнение) |
| `terrain.river_reach_km` | 1 | км | Ближе реки узел всегда `water = river` |
| `biome.vein_k` | по биому | × | Во сколько раз узел биома чаще несёт жилу (D-321; задумывалось как «terrain.mountain_vein_k» = 2 для гор) |
| `road.wild_multiplier` | 4 | × | Бездорожье: медленнее нынешних 2,5 тропы |
| `path.wear_threshold` | 20 | проходов | Столько износа делает бездорожье тропинкой |
| `path.fade_threshold` | 8 | проходов | Ниже этого тропинка зарастает обратно |
| `path.fade_per_day` | 1 | проходов/сут | Сколько износа снимает суточный тик |
| `map.city_lat_max` | 60 | ° | Выше не закладываются города |
| `map.lattice_m` | 5 | м | Решётка планеты: ячейка — ключ найденного узла (D-321) |
| `biome.reach_m` | по биому | м | Как близко и далеко разведывают из узла биома (D-321) |
| `biome.swing_c` | по биому | °C | Суточный перепад температуры узла (D-321) |
| `biome.marks`, `biome.vein_k`, `biome.zonal`, `biome.azonal`, `biome.bounds` | по биому | | Признаки, жилы и границы классов поля (D-321; с волны 4 плана 12 границы — две таблицы, в `bounds` болото и берег) |
| `complex.chance`, `complex.schemes` | по планете и биому | | Как часто и какие комплексы находит разведка (D-321) |
| `map.memory_places` | 400 | шт | Потолок памяти X (§4) |
| `map.public_delay_days` | 3 | сут | Задержка публичной карты — возраст отдаваемого снимка |
| `map.sight_km` | 0,1 | км | Радиус видения вокруг тела (был `MAP_SIGHT` = 2 шага графа; 5 км до D-323, сто метров с его дополнением — город в секундах ходьбы) |
| `explore.window_km` | 2 | км | Окно прицела: что из поверхности читает прицел вокруг точки (волна 6; много шире любой дали) |
| `travel.walk_speed_kmh` | 4 | км/ч | Скорость хода по бездорожью до множителей D-107 |

`explore.*` кроме четырёх, которыми пользуется генерация (`node_area`,
`vein_richness`, `vein_stock`, `vein_share` — с волны 6 три последних и
доли леса, камней и луга зовутся `ground.*`), прежние секунды шага и пути
через город и цена дали (D-180) теряют смысл и уходят из реестра волной 1 вместе с кодом — одним движением, как требует CLAUDE.md про
удаление ключа.

---

## 9. Что меняется в вольте

- [90-production/02](02-decision-log.md): D-319 (это решение)
- [00-core/02](../00-core/02-open-questions.md): OQ-139…138
- [50-interface/05](../50-interface/05-map-and-movement.md): раздел D-237 —
  вращение по двум углам; «Разведка и туман» — память вместо тумана;
  «Космический масштаб» — подлёт и окно «Внутри»
- [10-world/07](../10-world/07-map-topology.md): слои как полосы масштаба,
  скрытые места, конечная площадь
- [10-world/06](../10-world/06-space-and-travel.md): ссылка на зависимость от
  масштаба неба (волна 5)
- [20-systems/06](../20-systems/06-farming.md): свет и температура зависят от
  долготы
- [30-economy/07](../30-economy/07-constants.md): таблица §8
