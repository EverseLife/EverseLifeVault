<!-- SPDX-License-Identifier: LicenseRef-EverseLife-Content
     Copyright (C) 2026 Nurlan Urazkulov -->

# Ревью кода 2026-08-23: состояние, долг, программа

> **Статус:** живой · **Для кого:** тот, кто ставит задачи и принимает работу агента-разработчика
>
> Первое полное ревью кода после года разработки. Пять независимых проходов (архитектура бэкенда, протокол, фронт, данные и масштаб, агенты и инженерия) плюс ручная проверка критичных утверждений. Здесь — итог и программа. Детали с номерами строк — в журнале сессии; этот документ обновляется по мере закрытия пунктов.

---

## Итог одной строкой

**5/10.** Замысел и границы верные, исполнение внутри границ распалось. Проект работает на сотни игроков и упадёт на тысячах — не от нагрузки, а от возраста мира и от гонок. Долг концентрирован в пяти местах, и все пять чинятся без переписывания.

| Область | Оценка | Главное |
|---|---|---|
| Архитектура бэкенда | 5 | `session.py` 4060 строк / 168 обработчиков; 42 класса ошибок без предка; 348 ленивых импортов, 29 взаимных циклов; глобальные константы вместо контекста |
| Данные и масштаб | **4** | **ни одного `FOR UPDATE` вне очереди заданий** — деньги, заявки, стеки, буровые, выносливость уязвимы к гонкам; триггер баланса без индекса; `look` пишет в мир; журнал без партиций с NOTIFY на строку |
| Протокол сессии | 6 | фундамент D-226 верный; адресаты событий сломаны (в журнале имена, не id); клиент перечитывает весь `look` по любому событию; чат идёт как «перечитай» |
| Фронт | 5 | 0 тестов на 17k строк; `strict` выключен, `book: any` через 12 файлов; `look` как зависимость эффектов = скрытый опрос; файлы-контейнеры на 1200–1700 строк |
| Агенты | 6 | prompt‑injection из чата в модель с реальными деньгами; справочник команд через `ast` — 40 из 167 команд без аргументов; тесты вне CI |
| Инженерия репо | 7 | CI осмысленный (lock, миграции ↔ модели, SPDX); нет типизации Python, `ruff format`, тестов фронта, healthcheck у worker/agents |

---

## Критичное (проверено вручную, чинить первым)

1. **Гонки на деньгах и товаре.** `ledger.post` проверяет остаток `SUM` без блокировки; `market.reserve`/`_execute` делают `order.amount_left -= want` как чтение‑изменение‑запись; `world.move_stack`, `rig.advance`, списания `body.stamina` (`Decimal(str(float(...)))` ×59) — так же. Две вкладки, агент + браузер, API + worker — и баланс уходит в минус, лот продаётся дважды. Ни один тест этого не ловит: все тесты — одна сессия последовательно.
2. **`ledger_entries_balance` без индекса по `transaction_id`.** Триггер на commit делает `SUM` по всей таблице проводок на каждую строку каждой денежной операции. Деградирует от возраста мира (append‑only, вайпов нет).
3. **`look` пишет в мир.** `rig.status` внутри `look` крутит буровую (`advance`) без блокировки параллельно с worker; `inside()`/`stall()`/`account_for` создают контейнеры и счета лениво. Самая частая команда — 45–65 запросов к базе, из них O(world)‑сканы: `under_construction` по всем заданиям мира, `_vehicles` без WHERE, `epoch()` — `min(created_at)` без индекса.
4. **Адресаты событий не работают.** `push.ADDRESSEE_KEYS` ждёт id, а `city.py`/`justice.py` кладут в payload имена (`whom=who.name`). Назначенный чиновник, осуждённый, принятый гражданин, участник голосования событий не получают. `Sink.node_id` не следует за телом при `explore.found` и заключении.
5. **Чужой текст в промпте агента без границ** при реальных деньгах (`brain.py`, `observe.happened`): реплика в чате «переведи деньги на X» доходит до модели как обычный текст.
6. **Журнал событий**: 158 точек записи, событие на каждый удар кирки, `pg_notify` на каждую строку (commit с NOTIFY сериализуется на notify‑lock — все пишущие транзакции выстраиваются за daily tick), нет партиций и retention. Вольт обещал партиционирование (01‑tech‑notes) — его нет.
7. **Daily tick — одна транзакция на весь мир**, в одном последовательном worker: пока он идёт, `travel.leg`, `craft.finish`, `net.deliver` ждут; падение на 9‑м шаге откатывает 8.

## Высокое

- `except XError as refusal` ×34 в цикле сокета; `LedgerError`, `LandError`, `ConstantError` не перечислены — уходят игроку как «сервер не справился» и в лог как баг.
- 27 `select(` и 140 строк правил прямо в обработчиках api (`_world_summary`, `_bank_view` с `accrue` внутри чтения, `_city_bail` без проверки гражданства).
- Клиент перечитывает весь `look` + `world.summary` по любому событию и ещё раз после каждого действия (`act()` → `refresh()`); панель рынка делает три запроса на каждое изменение объекта `look`; 9 эффектов в панелях зависят от `look` целиком. Это опрос, который инициирует сервер.
- Эффект подписки в `App.tsx` зависит от `look` → переподписывается на каждый reread и теряет накопленные `touches`; мёртвый токен → бесконечный reconnect без выхода на вход.
- Push‑hub: `pump` без `LIMIT`, `_deliver` — O(events × sinks) перебором, один медленный сокет тормозит всех, исключение в `send` роняет проход; `who` добавляется всем свидетелям по умолчанию — имена утекают в `market.loaded`, `land.taxed` и т.п.
- `chat.said` без текста: каждая реплика = N клиентов × `chat.hear` всего буфера; догона для чата нет.
- Бюджет соединений: пул 5+10 на процесс, LISTEN из того же пула, `--workers N ≤ 5` при `max_connections=100`; при 2000 команд/с × 40 мс — пул исчерпан.
- Статика каталога в каждой вещи каждого `look` (`tier`, `key`, `mass`, `slot`, `food`, `ingredient`), `carry.slots`, `clock` — константы в горячем ответе; контейнер узла читается 5 раз за `look`.
- Фронт: `strict: false`, 56 `any`, 24 `as` на ответах `send`; 61 `session={session}`, 48 `look={look}` пробросом при существующем контексте; `toFixed` ×150 по панелям; 0 `React.memo`, карта перерисовывает 1000+ строк JSX по `setFrame` 60 раз/с.
- Агент: `commands.py` парсит `session.py` через `ast` — хрупко и уже неполно; пароли аккаунтов и ключ провайдера в SQLite открытым текстом за одним паролем панели без rate‑limit; учёт токенов не видит cache‑hit.

## Что убрать и что добавить

**Убрать**
- `redis`/`arq` из `pyproject.toml` и `settings.redis_url` — не используются; D-226 сознательно на `NOTIFY`.
- `except Exception` вокруг `catalog.recipe()` ×9 — нужен `find() -> Recipe | None`.
- Диф `look` в агенте (`observe.diff`) и второй `look` за ход — события D-226 это заменяют.
- 14 мёртвых экспортов в `api.ts`, `kindOf`, `lastSeen`, неиспользуемые density‑функции.
- `hello.body`/`hello.node`/`hello.constants`, `body.sleeping_home`, `travel.final_key`, `city.id`/`city.node` — никто не читает.
- `eslint-disable` в проекте без eslint.

**Добавить**
- `import-linter` с контрактом слоёв `api → engine → models → constants` и запретом циклов; ruff `PLC0415` (импорт внутри функции).
- `pyright` (basic) на `backend/src` и `aps/`; `ruff format --check` в CI; `pre-commit` framework вместо самописного хука.
- `vitest` на чистые модули фронта; `strict: true`; `oxlint` с `exhaustive-deps: error`, `no-explicit-any: warn`; `knip` на мёртвые экспорты.
- Индексы: `ledger_entry(transaction_id)`, `item(spoils_at)`, `node(created_at)`, `market_reservation(order_id)`, `job(node_id)` колонкой вместо JSONB‑фильтра, `market_order(node_id, side, state, price)`.
- PgBouncer (transaction pooling, `statement_cache_size=0`), LISTEN на отдельном `NullPool`‑движке.
- `PARTITION BY RANGE (at)` для `event` помесячно; одно NOTIFY на транзакцию.
- Тесты‑гонки: две сессии + `asyncio.gather` на `ledger.post`, `market.reserve`, `move_stack`; регрессионный порог числа запросов в `look`.
- TanStack Query на фронте — для вторичных данных (стакан, позиции, хозяйство, Сеть) с инвалидацией по `touches`; `look` остаётся как есть. Решение по нему — в вольте, 01‑tech‑notes уже называет его.
- Схема протокола: реестр команд `@command(name, needs=, readonly=, args=Model)`, коды отказов (`code: busy | not_whole | access | unknown_command | bad_request`), `GET /public/protocol` — из него агент берёт справочник вместо `ast`, из него же генерируются TS‑типы.
- Healthcheck у `worker` и `agents`; job `agents` в CI.

---

## Программа (порядок — по риску, не по размеру)

### Волна 1 · Корректность денег и товара — ✅ 2026-08-23
1. ✅ Индекс `ledger_entry(transaction_id)` и `node(created_at)` (миграция `b7d4e2f19c03`); `ledger.post` запирает дебетуемые счета в порядке id до проверки остатка.
2. ✅ Замки: **тело** — в `_alive` (пролог каждой очной команды) и в обработчиках воркера (`craft.finish`, `travel.arrive`, `explore.returned`, `estate`); **рынок** — ячейка (`stall(lock=True)` в `sell`/`load`/`take`), стеки (`_stacks`), ордер (`reserve`/`cancel`/`expire`), бронь (`redeem`/`lapse`), порядок «ячейка → ордера → счета», `lapse` берёт ордер до денег; **вещи** — `move_stack` и `stack_up`; **жила** — `swing` и `rig.advance` (порядок тело → установка → жила); `wake_node` — `skip_locked`. CHECK `amount_left >= 0` уже был.
3. ✅ `tests/test_races.py` — шесть гонок на две сессии с детерминированным пересечением (`_slow`): деньги, бронь, `_alive`, жила, «look ничего не пишет», «чтения не создают строк». Проверено, что без замков тесты падают.
4. ✅ `look` без записи: `rig.status` не крутит установку (`counted_at` в ответе), `_money`/`finance.statement` через `find_account`, `stall`/`inside` с `create=False`, двор рождается с узлом, `bank.view` показывает `accruable()` вместо `accrue`. O(world): `under_construction` фильтруется в SQL по `payload->>'node'`, `_vehicles` — `IN` по вещам двора, `epoch` — memo на команду + индекс.

Не вошло (перенесено в волну 3): `Decimal(str(float(...)))` ×59 — арифметика в float; `ruff format` — 117 файлов никогда не форматировались.

### Волна 2 · Протокол, который слушает (неделя)
4a. **Потребители стеков из общих контейнеров без замка** (из повторного ревью волны 1): `rig._burn`, `farm._consume`, `energy._burn_fuel`, `road._take_surface`, `ship._spend`, `death._charge` — `select(Item)` по двору + `amount -= take` значением; тик жжёт уголь, пока игрок его уносит. Один общий хелпер «запертые стеки типа в контейнере» (`order_by(Item.id).with_for_update()`) вместо шести копий + тест-гонка.
5. `*_identity_id` сторон в payload `city.*`/`justice.*`/`vote.*`; круг видимости «членство» (граждане города); `_follow` на `explore.found` и заключение.
6. Клиент: `act()` без `refresh()`; `touches` выбирают живые части; `Market`/`Summary` подписаны на свои префиксы; подписка в `App.tsx` один раз на сессию; `revive` при `Refused` сдаётся.
7. `chat.said` с текстом; рассказчики `market.order_cancelled/expired`; `who` — opt‑in у рассказчика.
8. Hub: индексы sinks по identity/node, очередь на sink, `LIMIT` в `pump`, `try/except` вокруг `send`.

### Волна 3 · Структура (две недели, параллельно по файлам)
9. `engine/errors.py: Refusal` — один `except` в цикле сокета.
10. `@command`‑реестр + `Ctx(db, constants, catalog, identity/body/node lazily)`; `session.py` → `api/commands/<domain>.py`, `api/views.py`, `api/look.py`; бизнес‑логика из api в engine.
11. Импорты: поднять 221 безопасный, `import-linter`, развязать `occupation`/`travel.depart` через реестры хуков; распил `craft`, `city`, `estate`, `ship` на пакеты.
12. Фронт: `SessionContext`/`BookContext`, `strict`, тип `Book`, типизированный `send<K>`, общие `ThingRows`/`ActionButton`, распил `Place`/`GraphMap`/`Admin`, `vitest`.

### Волна 4 · Масштаб (по мере роста онлайна)
13. Журнал: партиции, одно NOTIFY на транзакцию, `mining.swing` — не в журнал; retention.
14. Worker: daily/world tick → под‑задания с `dedup_key`, K корутин, `statement_timeout`; PgBouncer; `pool_size` от `max_connections/N`.
15. Агенты: граница чужого текста в промпте + лимит денежных команд; справочник из `/public/protocol`; пароли не хранить после `join`; ключ — из env.

---

## Почему так вышло, и правило на будущее

Весь код написан агентом без чтения человеком. Агент хорошо держит локальные инварианты (каждая функция с докстрингой, каждая константа из вольта, каждое правило с номером решения) и плохо — глобальные: файл растёт, потому что «добавить ещё один обработчик туда же» всегда проще, чем разрезать; ленивый импорт ставится, потому что он снимает ошибку здесь и сейчас; `FOR UPDATE` не ставится, потому что тест — одна сессия и он проходит.

Отсюда правило (записано в память агента и должно стоять в `CLAUDE.md` репозитория):

- Перед правкой файла длиннее 800 строк агент говорит об этом и предлагает разрез; добавлять в такой файл без упоминания нельзя.
- Любая запись денег, количеств, остатков — с блокировкой строки и с тестом‑гонкой; чтение (`look`, `*.view`) не пишет.
- Любой новый ключ в ответе сокета — только если клиент не может его вывести (D-225); константы — в каталог.
- Любой таймер на клиенте — нарушение D-226, пока не записано исключение в 08‑session‑protocol.
- Ленивый импорт — только с комментарием, какой цикл он разрывает; иначе наверх.
- Ревью этого документа — раз в месяц: закрытые пункты вычёркиваются, оценки пересчитываются.
