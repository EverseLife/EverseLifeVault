# SPDX-License-Identifier: AGPL-3.0-only
# Copyright (C) 2026 Nurlan Urazkulov

"""Поле планеты: рельеф, сделанный процессами, а не шумом (90-production/12).

Вольт делает поле, игра его читает. Конвейер один на все планеты, рецепт
планеты — параметры (§4.7 плана): плиты и порода (`plates`), сток (`hydro`),
эрозия (`erosion`), климат (`climate`), формы (`forms`); собирает всё
`pipeline`, кладёт файлом `store`, а `census` и `render` — линейки, которыми
поле судят до того, как оно попадёт в игру (§4.4).

Только numpy: сборка вольта идёт там, где нет ни scipy, ни PIL, — в CI и в
контейнере редактора. Числа мира (шаг сетки, доля моря, размах) приходят из
`build/constants.json` (D-065); коэффициенты самих процессов — свои у каждого
модуля, как октавы у шума: это математика, а не баланс.
"""
